<Window x:Class="Freexcel.App.Host.PivotLabelFilterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Label Filter"
        Width="340"
        Height="222"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <DockPanel Margin="14">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Width="74" Margin="0,0,8,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="74" IsCancel="True"/>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="104"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Condition:" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <ComboBox x:Name="LabelFilterKindBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Value:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="LabelFilterValueBox" Grid.Row="1" Grid.Column="1"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="And:" VerticalAlignment="Center" Margin="0,8,8,0"/>
            <TextBox x:Name="LabelFilterValue2Box" Grid.Row="2" Grid.Column="1" Margin="0,8,0,0"/>
        </Grid>
    </DockPanel>
</Window>
