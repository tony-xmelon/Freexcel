<Window x:Class="Freexcel.App.Host.PivotFieldFilterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter"
        Width="320"
        Height="430"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="_OK"
                    Width="74"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="_Cancel"
                    Width="74"
                    IsCancel="True"/>
        </StackPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0"
                   Content="_Search:"
                   Target="{Binding ElementName=FilterSearchBox}"
                   Padding="0"
                   Margin="0,0,0,4"/>
            <TextBox x:Name="FilterSearchBox"
                     Grid.Row="1"
                     Margin="0,0,0,8"
                     TextChanged="FilterSearchBox_TextChanged"
                     AutomationProperties.Name="Search PivotTable filter items"/>

            <CheckBox x:Name="SelectAllCheckBox"
                      Grid.Row="2"
                      Content="Select _All"
                      Margin="2,0,0,6"
                      Click="SelectAllCheckBox_Click"/>

            <ListBox x:Name="FilterItemsList"
                     Grid.Row="3"
                     BorderBrush="#BDBDBD"
                     AutomationProperties.Name="PivotTable filter items">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Caption}"
                                  IsChecked="{Binding IsChecked}"
                                  Click="FilterItemCheckBox_Click"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </DockPanel>
</Window>
